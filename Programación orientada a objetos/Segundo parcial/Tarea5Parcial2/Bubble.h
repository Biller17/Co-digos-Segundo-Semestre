#pragma once
#include "Sort.h"

template <class T>
class Bubble : public Sort<T>
{
public:
    T* sort(T*);
    
    
};

template <class T>
T* Bubble::sort(T*o)
{
    int i = 0;
    if(T[i] < T[i+1])
    {
        
    }
}